model:
  repo_id: Qwen/Qwen3-8B-Base
  # Pinned at first download per protocol §4.1 (was REPLACE_WITH_EXACT_COMMIT_HASH).
  revision: 49e3418fbbbca6ecbdf9608b4d22e5a407081db4
  max_seq_length: 4096

adapter:
  kind: lora
  r: 16
  alpha: 32
  dropout: 0.05
  target_modules:
    - q_proj
    - k_proj
    - v_proj
    - o_proj
    - gate_proj
    - up_proj
    - down_proj
