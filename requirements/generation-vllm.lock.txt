# Isolated generation/rollout environment lock — vLLM SERVER mode (protocol §3).
#
# PURPOSE. This env is NEVER installed into the training runtime. It lives in
# its own virtualenv (scripts/launch_trl_vllm_server.sh) so vLLM may pin its
# own torch/CUDA stack — x18 (runs/x18_vllm_probe.json) proved every vLLM
# release pins its own exact torch, so environment separation is the only
# correct integration on the Colab G4 image (torch 2.11.0+cu128 image-owned).
#
# PIN RULES (learned from the 2026-08-15 server smoke failure):
#   1. vllm must be INSIDE TRL's supported window. trl 1.9.0 supports
#      vLLM 0.17.0..0.25.1 — vllm 0.27.1 failed (unsupported + flashinfer
#      TypeError). Pin the TOP of the supported window, not the global latest.
#   2. trl== must EQUAL the training env's pin (colab-g4.lock.txt: trl==1.9.0)
#      so the vllm-serve client/server protocol matches.
#   3. venv Python must satisfy requires-python >=3.12 (the launch script now
#      forces 3.12; 3.11 triggered flashinfer's `array.array[int]` TypeError).
#   4. Exact `==` pins only. No ranges.
#
# Blackwell note: SM 12.0 needs CUDA >= 12.9 inside this env's stack; vLLM
# 0.25.x ships cu129/cu130-capable wheels. If 0.25.1 still fails on SM 12.0,
# walk DOWN the supported window (0.25.0, 0.24.x, ...) and record the first
# version that serves + generates; commit that pin with the smoke log.

vllm==0.25.1
trl==1.9.0
